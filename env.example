# Docker Compose переменные окружения
# Скопируйте этот файл в .env при необходимости

# GitLab Registry (автоматически устанавливается в CI/CD)
CI_REGISTRY=git.edimdoma.ru:5050
CI_COMMIT_TAG=latest

# Node.js окружение
NODE_ENV=production

# Порт фронтенда (по умолчанию 8088)
FRONTEND_PORT=8088

# Для локальной разработки оставьте эти переменные пустыми или используйте localhost
# CI_REGISTRY=localhost
# CI_COMMIT_TAG=latest

