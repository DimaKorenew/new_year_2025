# 🚀 Как запустить проект

## Быстрый старт

```bash
cd /Users/dmitrij/Desktop/new_
npm run dev
```

Сервер запустится на: **http://localhost:5173/new-year-2025/**

## Проверка работы сервера

1. **Проверьте, что сервер запущен:**
   ```bash
   lsof -ti:5173
   ```
   Если команда вернет номер процесса - сервер работает.

2. **Откройте в браузере:**
   - http://localhost:5173/new-year-2025/
   - или http://localhost:5173/ (может перенаправить)

3. **Если не открывается:**
   - Проверьте, что порт 5173 не занят другим приложением
   - Попробуйте очистить кеш браузера (Ctrl+Shift+R или Cmd+Shift+R)
   - Проверьте консоль браузера на ошибки (F12)
   - Убедитесь, что файрвол не блокирует подключение

## Альтернативные команды

```bash
# Обычный dev server (быстрый, без SSR)
npm run dev

# SSR сервер (для production-подобного режима)
npm run dev:ssr
# Работает на http://localhost:5174
```

## Остановка сервера

Нажмите `Ctrl+C` в терминале, где запущен сервер.

Или:
```bash
pkill -f vite
```

