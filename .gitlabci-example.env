# Примеры GitLab CI/CD переменных
# Эти переменные нужно установить в: Settings → CI/CD → Variables

# SSH ключ для доступа к серверам (обязательно, protected)
SSH_PRIVATE_KEY=<your-private-ssh-key>

# Docker Registry credentials (обязательно, protected)
CI_REGISTRY=git.edimdoma.ru:5050
CI_REGISTRY_USER=gitlab-ci-token
CI_REGISTRY_PASSWORD=<your-registry-password>

# Опциональные переменные
FRONTEND_PORT=8088
NODE_ENV=production

