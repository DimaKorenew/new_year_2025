# Быстрый старт: SSG/ISR/SSR

## ✅ Что реализовано

1. **SSG** - статические страницы предрендерятся при сборке
2. **ISR** - динамические страницы кэшируются и регенерируются
3. **SSR** - автоматически для краулеров, обеспечивая одинаковый контент

## 🚀 Быстрый старт

### 1. Установка зависимостей
```bash
npm install
```

### 2. Разработка
```bash
# Обычная разработка
npm run dev

# С SSR
npm run dev:ssr
```

### 3. Сборка
```bash
npm run build
```

Это выполнит:
- `build:client` - сборка клиентского бандла
- `build:server` - сборка серверного бандла
- `build:prerender` - предрендеринг статических страниц

### 4. Запуск SSR сервера
```bash
npm run preview:ssr
```

## 📋 Основные файлы

- `src/entry-client.tsx` - клиентский entry (гидратация)
- `src/entry-server.tsx` - серверный entry (SSR)
- `server/ssr.js` - SSR сервер
- `server/isr.js` - ISR middleware
- `scripts/prerender.js` - скрипт предрендеринга

## 🔧 Конфигурация

Переменные окружения (опционально):
- `PORT` - порт SSR сервера (по умолчанию 5174)
- `ISR_REVALIDATE` - время жизни ISR кэша в секундах (по умолчанию 3600)
- `FORCE_SSR` - принудительный SSR для всех запросов (true/false)

## 📖 Подробная документация

См. `SSR_SETUP.md` и `README_SSR.md` для детальной информации.

