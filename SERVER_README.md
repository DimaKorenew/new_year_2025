# 🚀 Backend Server для списка покупок

Локальный Express.js сервер для синхронизации расшаренных списков покупок между браузерами.

## 📋 Требования

- Node.js 18+ 
- npm или yarn

## 🛠 Установка

1. Установите зависимости для backend сервера:

```bash
cd server
npm install
```

2. Установите зависимости для корневого проекта (если еще не установлены):

```bash
npm install
```

## 🚀 Запуск

### Вариант 1: Запуск только backend сервера

```bash
npm run dev:server
```

Сервер запустится на `http://localhost:3001`

### Вариант 2: Запуск только frontend

```bash
npm run dev:client
```

Frontend запустится на `http://localhost:5173`

### Вариант 3: Запуск обоих одновременно (рекомендуется)

```bash
npm run dev:all
```

Это запустит и backend, и frontend одновременно.

## 📡 API Endpoints

### POST /api/lists/share

Создает новый расшаренный список.

**Request:**
```json
{
  "items": [
    {
      "id": "1",
      "recipeId": "5",
      "recipeName": "Имбирное печенье",
      "ingredientName": "мука",
      "amount": "200 г.",
      "checked": false
    }
  ],
  "recipes": ["5"]
}
```

**Response:**
```json
{
  "shareId": "abc12345",
  "url": "http://localhost:5173/s/abc12345",
  "expiresAt": 1700000000000
}
```

### GET /api/lists/share/:id

Получает расшаренный список по ID.

**Response:**
```json
{
  "items": [...],
  "recipes": ["5"],
  "metadata": {
    "createdAt": 1700000000000,
    "updatedAt": 1700000000000
  },
  "viewsCount": 5
}
```

**Ошибки:**
- `404` - Список не найден

### PATCH /api/lists/share/:id

Обновляет расшаренный список.

**Request:**
```json
{
  "items": [...]
}
```

**Response:**
```json
{
  "items": [...],
  "recipes": ["5"],
  "metadata": {
    "createdAt": 1700000000000,
    "updatedAt": 1700000001000
  }
}
```

**Ошибки:**
- `404` - Список не найден
- `400` - Неверный формат данных

## 💾 Хранение данных

Сервер использует **in-memory хранилище** (Map в памяти). Это означает:

- ✅ Быстрая работа для локальной разработки
- ✅ Не требует настройки базы данных
- ❌ Данные теряются при перезапуске сервера
- ❌ Не подходит для production

Для production рекомендуется использовать:
- MongoDB
- PostgreSQL
- Redis
- Другие персистентные хранилища

## 🔄 Синхронизация

Frontend автоматически синхронизируется с backend:

1. **При изменении товара** (добавление/удаление/отметка):
   - Изменения сохраняются в LocalStorage мгновенно (для быстрого UI)
   - Отправляется PATCH запрос на сервер с debounce 1 секунда

2. **Периодическая проверка обновлений** (polling):
   - Каждые 5 секунд делается GET запрос к серверу
   - Если серверная версия новее → обновляется UI
   - Показывается toast уведомление о новых изменениях

## 🌐 CORS

Сервер настроен для работы с frontend на `http://localhost:5173` (Vite dev server).

Для изменения порта или домена, отредактируйте настройки CORS в `server/index.js`:

```javascript
app.use(cors({
  origin: 'http://localhost:5173',
  credentials: true
}));
```

## 📝 Логирование

Сервер выводит в консоль:

- `🚀 Server running on http://localhost:3001` - при старте
- `✅ Created list with ID: abc12345` - при создании списка
- `📖 Retrieved list: abc12345 (views: 5)` - при получении списка
- `🔄 Updated list: abc12345` - при обновлении списка
- `❌ List not found: abc12345` - при ошибке 404

## 🐛 Обработка ошибок

### Офлайн режим

Если backend недоступен, frontend автоматически переключается в fallback режим:

- Использует LocalStorage для хранения
- Кодирует данные в URL параметры (base64)
- Продолжает работать, но без синхронизации между устройствами

### Сетевые ошибки

При проблемах с сетью:

- Показывается toast: "📡 Нет связи. Работаем в оффлайн режиме"
- Изменения сохраняются локально
- При восстановлении связи → автоматическая синхронизация

## 🔧 Настройка портов

По умолчанию:
- Backend: `3001`
- Frontend: `5173` (Vite)

Для изменения порта backend, отредактируйте `PORT` в `server/index.js`:

```javascript
const PORT = 3001; // Измените на нужный порт
```

## 📦 Структура проекта

```
new_/
├── server/
│   ├── index.js          # Express сервер
│   └── package.json      # Зависимости backend
├── src/
│   ├── services/
│   │   └── shoppingListApi.ts  # API клиент
│   └── ...
└── package.json          # Зависимости frontend
```

## 🚨 Важные замечания

1. **Данные в памяти**: При перезапуске сервера все расшаренные списки удаляются
2. **Локальная разработка**: Сервер предназначен только для локальной разработки
3. **Безопасность**: В production необходимо добавить:
   - Аутентификацию
   - Валидацию данных
   - Rate limiting
   - Персистентное хранилище

## 📚 Дополнительные ресурсы

- [Express.js документация](https://expressjs.com/)
- [CORS настройка](https://expressjs.com/en/resources/middleware/cors.html)
- [Nanoid для генерации ID](https://github.com/ai/nanoid)














